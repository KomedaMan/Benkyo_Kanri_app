## HTML おさらい
### 文字の扱い方
<b>太字 Bold</b>

<strong>強調</strong>

こと上記においてはどちらでもいいと思う。


<h2 style="color:red;">色付きの見出し color-tinted headline</h2>
<p style="color:green;">色付きの段落 color-tinted paragraph</p>
<span style="color:blue;">色付きの文字範囲 color-tinted span</span>

<p style="background-color: yellow;">黄色い背景</p>
<div style="background-color: lightblue;">水色の背景のボックス</div>

<p style="color: white; background-color: black;">
  白い文字に黒い背景
</p>

カラーコード指定もできる
<p style="color: #ff0000;">赤い文字（#ff0000）</p>

改行<br>マークダウンの\を入れることで行末ならば改行できる\
改行



### リストの作成
<ul>
   <li>牛乳</li>
   <li>砂糖</li>
   <li>小麦粉</li>
</ul>

順序なしのリストを作れる

<ol>
  <li>いちご</li>
  <li>りんご</li>
  <li>みかん</li>
</ol>
これは順序ありのリストを作れる。
正直、このファイルで使うならば下のように書いたほうが簡単

1. リンゴ
2. イチゴ
3. ミカン

<dl>
  <dt>名前<dt>
  <dd>HANA</dd>
  <dt>居住地域<dt>
  <dd>大阪</dd>
  <dd>福岡</dd>
</dl>

dlはリストの定義（definition list）、dtはリストの項目（definition team）、ddはリストの値（definition description）。

### 画像を添付する場合
<img src="/docs/test.png" alt="テスト画像" width="200">\
<img src="test.png" alt="テスト画像" width="200">\
一応widthだけ指定すれば縦横比を保ったまま拡大縮小してくれるが、heightも指定することで縦横比を変えられる。
alt属性は画像のタイトルみたいなもので、画像が表示されない場合にテキストが記載されるらしい。

Shield

<img src="https://img.shields.io/badge/-{言語、フレームワーク名など}-{シールドのカラーコード}.svg?logo=next.js&style={バッチのスタイル}&logoColor={ロゴのカラーコード}">

### リンク
<a href="https://www.google.com">Googleへ飛ぶ</a>\
あまり使いたくない。
Gitとかの成果物を記載したいときや出典を記載したいときに使う？

<blockquote>引用するときはこれを使うといい</blockquote>


### 特殊文字
&#34;あるいは&quot;と記述するとダブルクォーテーションになる

<blockquote>
   文字	    文字名	文字コード
   “	    &quot;	&#34;
   &	    &amp;	&#38
   <	    &lt;	&#60
   >	    &gt;	&#62
   スペース	&nbsp;	&#160
   ©	    &copy;	&#169
</blockquote>



## 要旨
応用情報試験の勉強に際して日々の正答率を管理するためのアプリを作成する
応用情報過去問道場の正答率は総合したものは出るが、日々の正答率は出ない。
そこで、やった日と現在の問題数と正答数を記載することでその日の正答率を出すようにする
また日々の変遷を折れ線グラフで表示することができるとなおよい。

[MUST]
1. 日付を入力できる
2. 解いた問題数（総合）正答した問題数（総合）を入力できる
3. 昨日との差分から今日の解いた問題数と正答した問題数、そして正答率を出力する

[WANT]
1. 出力（日付と問題数、正答数、正答率）をデータベースに保存する
2. 日々の正答率をグラフ化する

①目的
日々の勉強記録の確認

②ターゲット
自分

③解決する問題
日々解いた問題に対する正確な正答率を導き出すのが面倒なので、解いた問題数と正答数を入力するだけで確認できるようにする

④最低限の機能
日付、解いた問題数と正答数の入力
今日の解いた問題数と正答数、正答率の出力

⑤実装しない機能
過去問道場との連携

⑥入力/出力/保存
入力:日付、問題数、正答数などのテキスト
出力:今日の解いた問題数と正答数などのテキスト
保存:データベース化するならばjson?かSQLite他検討中

⑦画面イメージ
...

⑧作成順番
(動作フロー作成)
(画面イメージ作成)
1. 入力と出力がただしくCUIでできる
2. 入力フォームをGUIで作成
3. 出力がGUIで正しくできるか確認
   (ここまで正しく動作するか確認でき、この先作成できそうなら)
4. 出力内容をデータベースに保存する
5. データベースの内容をグラフ化できるようにする
6. 出力したグラフと最初の入力フォームの紐づけ
7. データベースの管理ができるようにする（初期化とか削除とか？）

⑨完了要件
上記作成順番の3もしくは7が達成されたとき
正常動作すること

⑩改善点
...

## =============================================
## MEMO
## =============================================

### roundとdecimal
roundは偶数への丸めのため一般的な四捨五入ではない。
偶数への丸めは少数部分xとし、整数部分をyとすると\
x < 0.5 ならば x = 0（切り捨て）\
x > 0.5 ならば x = 1　とする（切り上げ）\
<span style="color: red;">x = 0.5 ならば ｙが偶数になるほうに丸める。</span>yが偶数ならば切り捨て、yが奇数なら切り上げになる。

そのためここではdecimalを使わなければならない。

### 関数のかっこ
returnや出力などで関数名に()をつけるかつけないかは値を表すか、関数そのものを表すかによって使い分ける
すぐに関数の処理によって返される値を使いたい場合は()をつけてよい。
すぐに関数の処理をしては困る場合、（一般的にはGUIのボタンとか？）()をつけない方法で記載する。
変数にも関数を入れることができるが、その場合は変数に()をつけないと返り値が返されない（function型として扱われる）
v3時点ではどちらでもよいが、すぐに値がわかる()をつけて処理している。
後々関数として処理したほうが良い場合はそこを変更する必要がある。

### tkinter

1. まず、importする。as tkとしてもよい
2. rootを作る。画面を作成する。
3. 最後にメインループを作っておく。

Frameを使うと機能ごとや場所ごとにオブジェクトを管理出来て使いやすくなるかもしれない。

<pre>
root
|-Frame1
|   |-機能1
|   |-機能2
|
|-Frame2
|   |-機能3
|   |-機能4
|
</pre>

### padx、pady
padx=10とすると両方に10の空白を与えます。
左にだけ与えたい場合はpadx=(10, 0)とし、右にだけ与えたい場合はpadx=(0, 10)としましょう
右左はpadx=(右、左)で、上下もpady=(上, 下)で指定できる。

#### pack,grid,place
packは上下左右の方向に並べ、大まかなレイアウトに向く。
方向はanchorで指定し、anchor="news"の方角で表す。

gridは行・列の表形式で配置し、入力画面に向く。
row, columnで配置する

placeは座標で配置する。
x, y座標をたぶんピクセルで指定する。

### 関数内関数
GUI関数のところでは関数内関数を使った場所がある。
利点としてスコープが限定され、そのGUI関数内で使用可能な関数である。
意図しない呼び出し（いきなりメイン関数に呼び出されるなど）がなくなる利点がある。
頻繁に呼び出される関数であると、呼び出されるたびに内側の関数も再定義されるのっで、
処理のパフォーマンスに悪影響を及ぼす場合がある。

### .config
作成したオブジェクトの引数の値を変更できる。
今回は計算した後のテキストを消すために使った。
これをしないとラベルが延々に生成されたり、正常に表示されなかったりする。


### tkcalendar
ドキュメント
https://tkcalendar.readthedocs.io/en/stable/howtos.html
ターミナルでpip install tkcalendarを使用してインストールできる。
babelライブラリと依存関係にあるのでこれもインストールされる。
babelは日付や時刻を国際化、地域化して変換するライブラリでほか数字のフォーマットにも使用する。

入力欄はDateEntryで作成して通常のEntryと同じように使えるみたい。
引数のbackgroundはカレンダーの色、foregroundは月や年の文字の色が変わるっぽい？
locateでどこのカレンダーか指定できる。ja_JPは日本


## =============================================
## 問題点まとめ
## =============================================
v1-v3

    """
    !問題点
    roundでは正確な正答率がでない可能性がある。
    例えば100000問解いて正答数が66665問とすると小数点2桁で四捨五入すると66.67％とならないといけないが
    偶数丸めなので66.66％と出力される
    =>この例は現実的ではないが、正確性に欠けるためdecimalを使う方法に変える
    =>驚くことにこの例ではDecimalを使っても同じ結果になった。
        これは偶数丸めによるものではないようだ。
        どうやら2進数にしたときに有効桁で表現できないことによる誤差が生じるようである。（演算誤差）
    =>inputで入力された変数自体をDecimalで表現することにより、計算過程で2進数に変換されて生じる誤差をなくすことにした。
    """


v4

GUIの選定
Tkinter:Python標準、追加インストール不要で小規模なデスクトップアプリに向く。今回は超小規模のためこれでよい
Pyside/PyQt:高機能、大型アプリにも対応、ライセンスに注意する必要あり。今後本格的なアプリにするならばこれに手を付けるのもよい。
wxPython:OSみたいな見た目になる。情報量や利用者数が少ないらしい。
Kivy:様々なプラットフォームに対応しやすい。他UIとは見た目が異なる。これを経験するのもよい。
PySimpleGUI:簡単に作成できるらしい。小規模ツールに向く。今回のものに合致しているが、更新がもうない？

この中だと、Tkinter、Pyside、Kivyあたりがよいだろうか？完成品のイメージとしてはTkinterが小規模アプリに向いているので、これを採用したい。
他のプラットフォームと連携できるような仕組みにするならばKivy、大型アプリに発展するならばPyside/PyQtに移行する必要がありそう。
